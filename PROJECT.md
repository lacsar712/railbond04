# Railbond · 轨道电路回流键控与占用序

## 这不是什么

不是车票订票、不是列车时刻表前台、不是停车场计费。记录是 **轨旁轨道电路占用**：上下行区段、单调 feed 序号、防回滚。

## 谁在用

城轨联锁旁路维护台。区段占用上报带 `section=up|dn` 与单调 `feed=`。新 feed 必须不小于本区段已确认值，否则视为回退脏数据。

## 核心业务

1. 正文含 `section=up|dn` 与 `feed=` 非负整数。
2. 全局 setting `feed_min`：新 feed 必须 ≥ 该值；成功后更新。
3. 标签为轨道电路号 `tc-north` 等。
4. 附件是占用快照落地文件名（路径安全拼接）。
5. 短 ID 用于占用批次，写入审计。

## 运行与验收

- 先写 `feed=12` 成功，再写 `feed=4` 必须拒绝。
- 首页是轨旁维护台，不是订票页。